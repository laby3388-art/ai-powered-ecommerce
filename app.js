function getCookie(name){const row=document.cookie.split('; ').find(item=>item.startsWith(name+'='));return row?decodeURIComponent(row.split('=').slice(1).join('=')):''}
document.addEventListener('click',async event=>{const button=event.target.closest('[data-add]');if(!button)return;button.disabled=true;try{const response=await fetch(`/cart/add/${button.dataset.add}/`,{method:'POST',headers:{'Content-Type':'application/json','X-CSRFToken':getCookie('csrftoken')},body:JSON.stringify({quantity:1})});const data=await response.json();if(!response.ok)throw new Error(data.error||'Could not add this item.');const prior=button.innerHTML;button.innerHTML='Added to bag ✓';setTimeout(()=>{button.innerHTML=prior;button.disabled=false},1100)}catch(error){button.innerHTML=error.message;setTimeout(()=>{button.innerHTML='Add to bag ＋';button.disabled=false},1800)}})
